/**
 * @typedef {any} Row
 */

/**
 * @typedef {Object} Result
 * @property {Array<Row>} rows
 */

/**
 * @typedef {Object} Match
 * @property {number} id
 * @property {string} groupId
 */

/**
 * @typedef {Object} Pool
 * @property {ConnectCallback} connect
 * @property {EndCallback} end
 */

const __dummyModule = true;

export default __dummyModule;